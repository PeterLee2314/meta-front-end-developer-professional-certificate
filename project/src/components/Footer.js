import React, { useEffect, useRef } from "react";
import { Box, HStack, Link } from "@chakra-ui/react";

const Main = () =>
    {
        return (
            <footer>
                222
            </footer>
        );
    };
export default Main;
